## Finalize

1. **Recover MinerU extraction failures — if `ocr/figures.md` has a "⚠ Not extracted" section.** Each row there is a figure/table MinerU located but could not crop (recognizer failed), so it is missing from `ocr/figures/`. Recover each one:
   - Render its page from the source PDF: `pdftoppm -f <N> -l <N> -singlefile -r 200 -png <pdf-path> <tmp>/pg` (N = the row's `[pNN]`; the PDF is the source, not yet collected — use its current path). View the PNG.
   - If a real figure/table is there, crop it (caption included) and save it to the row's **Intended file** path under `ocr/figures/`. Crop with `pdftoppm ... -x -y -W -H` in pixels: page size in pts comes from `pdfinfo`; pixel = pt × DPI ÷ 72. The `bbox (approx)` is a hint in MinerU's own space — if it doesn't line up, crop by eye from the full-page render (papers pages are mostly whitespace around a table). Re-render and re-view until the crop is clean and complete.
   - Then edit `ocr/figures.md`: delete the row from the "⚠ Not extracted" section and add a normal row (Label / File / Page / Caption) to the main table. The recovered file is now an assigned figure like any other (step 2 will require it be explained).
   - If the region is a MinerU phantom (no real figure/table), just delete the row and note it in the manifest.
2. **Figure/table coverage — guarantee no orphan.** List `<WORK_DIR>/ocr/figures/` (now including any recovered files). For each file, confirm at least one report under `reports/` references it by its relative path (grep the reports for the filename). Any figure not yet referenced by an in-scope report — because its page (look it up in `ocr/figures.md`) fell outside every in-scope span, or a perspective missed it — must be added, with a one-line explanation and its `[pNN]`, to a "図表" section: in the perspective report whose span contains its page if that report is in scope, otherwise in `overview.md`. After this step **every file in `ocr/figures/` is referenced-and-explained by exactly one report** (when scope is "overview only", they all land in overview's 図表 section).
3. **Complete overview item 6**: read only the "次に読むべき論文" section of `reports/related-work.md` and copy the top 1–3 entries (verified ones first) into `overview.md`, replacing the placeholder. Keep any "(dblp未確認)" markers.
4. **Collect the PDF**: if the user said yes in Step 0, move the source PDF into the work dir as `<WORK_DIR>/<slug>.pdf`, renaming it to match the dir. Otherwise leave the source at its original path and name (do not rename a file that stays outside the work dir). (Do this *after* step 1, which still needs the PDF at its pre-collection path — or just use whichever path the PDF is at.)
5. **Consistency & faithfulness sweep — the whole-set check with teeth.** This is the one pass that reads the entire report set at once and checks it against the spine and the source; it exists because Phase 2's isolation means no writer ever sees the others, so cross-report contradictions and collective source-drift slip through every per-report self-check. It **subsumes** the old standalone diagram-order sweep and running-example-map correction — do not run those separately.
   - **Run the sweep by applying `paper-explainer-consistency-sweep`.** Under Claude Code, dispatch a single `paper-explainer-consistency-sweep` subagent so the whole set is read in an isolated context and only the findings return (this is what keeps orchestrator hygiene while still seeing everything). Otherwise, apply the skill inline as a self-contained pass and carry back only its findings. Provide it: the paths to every in-scope report, `<WORK_DIR>/source-structure.md`, `<WORK_DIR>/spine.md`, the source (`<WORK_DIR>/ocr/paper.md`), and the section map. It checks two mandates — (1) cross-report consistency (reports agree with each other and the spine on thesis direction, the future-direction conclusion, running-example attributions, headline numbers + scope, figure-derived facts) and (2) faithfulness to the source's logical structure (re-verifies both the source-structure artifact and the spine against the source — the single-source-error catch — and checks argument-chain preservation, author modality, scope conditions, non-collapsed axes, self-drawn-diagram order, and meta-declarations vs. the reality that follows them). It runs those checks over **every claim-bearing locus, not just the flowing prose** — table cells (each cell of a claims-vs-evidence table, enumerated individually and cross-checked against the report's own body, the spine, and the source on three axes: quantifier preserved, entity/class polarity not flipped, modality matching the body), bullets, trailing implementation/complexity/reproducibility sections, callouts, and diagram labels — because the disciplines get applied to the narrative first and drift survives in those structurally-secondary loci; a cell↔body internal contradiction (body correct, cell alone drifted) is a top-priority finding. It returns a findings list only; it does not edit anything.
   - **Apply the fixes yourself, targeted and source-anchored — do not regenerate reports.** For each `blocker` finding, use its locus + minimal-fix + source verdict (`[pNN]`) to make the smallest edit that moves the **wrong side to what the source supports** — a source-structure row, spine row, sentence, or table cell. Do not merely make two reports agree (that can harden the wrong claim); align to the source. Because the structure and factual spine are single-sourced, a wrong row is consistently wrong downstream — fix the authority artifact and every report that inherited it. Reading the flagged loci to edit them is the narrow read allowed by Context hygiene.
   - **Bound the loop.** After applying fixes, re-run the sweep once to confirm (at most 2 sweep rounds total). Stop when it returns clean or after the second round; report any residual `blocker` or `needs-human` findings to the user rather than looping further (an unbounded fix loop reintroduces the non-convergence this step is meant to end). `minor` findings may be left with a note.
6. **Print a manifest**: the work dir path (the renamed slug dir), each report,
   `source-structure.md`, `spine.md`, and `ocr/` contents (OCR always runs via
   MinerU). Note any figures recovered or dropped in step 1, and any residual
   sweep findings left unresolved in step 5.
7. **Point to the next steps** (do not run them unprompted): the reports can now become a two-speaker **audio guide** of the overview (`explainer-audio-dialogue` on `reports/overview.md` → `explainer-audio-narrate`) and a browsable **website** (`paper-explainer-generate-site`, the paper-tuned site builder — orders pages by perspective and labels them 背景 / 手法 / …, then `explainer-reading-site-deploy` to publish). To run the whole chain (summary → overview audio → site) in one shot, use `paper-explainer-full-guide`.

## Success criteria (verify the deliverable, not the steps)

- [ ] The work dir is named with the `{year}-{venue}-{short-title}` citation slug (lowercase `[a-z0-9-]`), and — if the user opted to collect the PDF — the PDF inside is `<slug>.pdf` matching the dir. (Exception: a name-collision the run reported, where the provisional name was kept.)
- [ ] `ocr/paper.md` exists with per-page `[pNN]` anchors (MinerU ran); every figure reference in the reports resolves as a relative path (`../ocr/figures/...` from `reports/`).
- [ ] `ocr/figures.md` exists and lists every file in `ocr/figures/` with its label, page (`[pNN]`), and caption; the figure files are named by paper number (`fig-NN` / `table-NN`). Its "⚠ Not extracted" section (if the converter wrote one) has been worked off at Finalize — every row either recovered into the main table (with the file now present in `ocr/figures/`) or dropped as a phantom and noted in the manifest.
- [ ] **Every file in `ocr/figures/` is referenced by at least one report and accompanied there by an explanation** (not embedded bare); no captured figure/table is left unexplained.
- [ ] `reports/overview.md` has the TL;DR + tag, the metadata block (no guessed venue/DOI), all six items filled, one key figure with a `[pNN]` caption, the 前提知識 and 原文の読み方 sections, pointer lines for every in-scope detail report, and a Section map whose `[pNN]` spans cover the whole body.
- [ ] `<WORK_DIR>/source-structure.md` exists and records only the paper's
      source-authored headings, in source order, with source-form titles, levels,
      and `[pNN]` anchors; it contains no report or perspective headings, and the
      Finalize sweep re-verified it against the source.
- [ ] The writing disciplines hold: item 1 has a one-sentence core definition; item 2 names concrete prior methods (no anonymous "既存手法"); item 4 gives numeric results, not adjectives only.
- [ ] The TL;DR (or a 総括 line) states the paper's own thesis — the overall verdict it leads with in the Abstract — and, when several results are reported, the relation the paper draws between them (superiority / asymmetry / trade-off), not just parallel numbers; that relation points the same direction as the source (not reversed).
- [ ] `<WORK_DIR>/spine.md` exists and holds the paper's confirmed facts (thesis + direction, and — where the paper has them — the future-direction conclusion, the figure-verified running-example map, and headline numbers with scope); it was built against the figures/Abstract (not a mental model) and handed to every perspective as the shared source.
- [ ] The overview's thesis and every detail report's stated conclusion agree — same claim and same direction — because all took the thesis (and the other shared facts) from `spine.md`; no two reports contradict each other on a spine fact.
- [ ] If the paper has a running example that more than one report discusses, the running-example map in `spine.md` was built by opening the original figure image (each row anchored to a named figure element in its evidence column, the figure winning over any conflicting OCR prose), and those reports attribute the example's features per that verified map — so the single shared source is correct, not merely consistent. (Single-sourcing hides a wrong attribution from cross-report checks, so the map's own figure-verification plus the Finalize sweep's re-verification are the checks that matter.)
- [ ] Every self-drawn diagram (`mermaid` etc.) in a report has its step order and causal direction matching that report's own prose and the source's explicit order — verified at Finalize by the consistency & faithfulness sweep, not only asserted at write time.
- [ ] The Finalize consistency & faithfulness sweep ran over the whole report set (subagent when available, else inline), its `blocker` findings were fixed by aligning the wrong side to the source (not by merely making reports agree), and any residual `blocker` / `needs-human` findings were reported to the user.
- [ ] Every headline number in the overview carries its scope, and subset averages are labeled as such: where per-unit averages are juxtaposed, each states whether its denominator is all N or a named subset ("among the K …"), and no subset average sits beside a whole-set average unlabeled.
- [ ] Overview claims preserve author modality: hedged or design-level claims (can / may / suggests / we believe) are not asserted as measured fact.
- [ ] The modality / quantifier / scope / attribution disciplines were applied to **every claim-bearing locus, not just the flowing prose** — table cells (each cell of a claims-vs-evidence table, cross-checked against that report's body and the spine), bullets, trailing implementation/complexity/reproducibility sections, callouts, and diagram labels — so no table cell or implementation note states a stronger or more-measured claim than the body and spine support.
- [ ] Each cell of every claims-vs-evidence table (e.g. discussion's 主張と根拠の対応表) agrees with that report's own body, the spine, and the source on three axes — quantifier preserved (no "majority / 大半" dropped into an all-cases claim), entity/class polarity not flipped (a negation-defined class such as "system that does not support X" not reversed), and modality matching the body (a hedged/design claim not asserted as measured) — with no cell-only drift from a correct body.
- [ ] Each report's meta-declarations match the reality that follows them: a heading or lead-in that announces an order / count / classification ("in order of decreasing X", "the following N points", "in order of importance") agrees with the actual ordering, number, and labels of the items beneath it — no declaration left stranded by a later reordering.
- [ ] The item-3 intuition (and any self-drawn figure) points the same direction as the paper's own figure/text — a trend or trade-off is not inverted — and does not contradict `method.md`.
- [ ] Every in-scope `reports/<perspective>.md` exists and carries `[pNN]` anchors.
- [ ] Every URL/DOI in `related-work.md` and overview item 6 traces to dblp output or the paper itself; unmatched entries carry "(dblp未確認)" and no URL.
- [ ] `<WORK_DIR>/paper.bib` exists with one BibTeX entry for the paper, either dblp-fetched or hand-built from printed metadata and marked `% not dblp-verified`; no venue/DOI/URL was invented, and overview links to it.
- [ ] A final manifest of all artifacts was shown to the user.
