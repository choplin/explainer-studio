# understanding (moved)

This former category has been split by responsibility:

- `html-docs` is now under `skills/html-docs/`.
- `reading-site-generate-base` is now under `skills/explainer/`.
- `diff-explainer` is now under `skills/diff-explainer/`.

No compatibility aliases are provided. Update references to the new skill names.
